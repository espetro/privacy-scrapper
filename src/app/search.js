

/**
 * Given the text inside the search box (HTML url), finds if it is an app and
 * retrieves its score, whether is computed or retrieved from a database.
 * If it is a web, scraps the web privacy policy first.
 */
function main() {

}